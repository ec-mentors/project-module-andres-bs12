{
  "name": "nutrition-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "NutritionTracker Full-Stack Web Application",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build"
  }
}
